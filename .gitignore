# Anchor build + ledger
.anchor
.anchor/program-logs
target
test-ledger

# Dependencies
node_modules
.yarn

# Rust backups
**/*.rs.bk

# Env files
.env
.env.local

# Build outputs
dist
build
*.tsbuildinfo

# Logs
yarn-error.log
npm-debug.log

# Solana keypairs (never commit private keys!)
id.json
**/*-keypair.json
target/deploy/*.json

# But keep Anchor IDLs (safe to commit, needed for clients)
!target/idl/*.json

# Coverage
coverage

# OS / Editor
.DS_Store
.vscode
.idea
